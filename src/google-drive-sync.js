// Mission Learning OS — Google Drive Sync v1.7
// Local-first cloud data, versioned backups, restore and multi-device sync.
// Persists the short-lived Google access token so opening another tab/reloading the app
// does not immediately force another Connect Google flow. Tokens are expired and removed
// automatically; no Google refresh token or client secret is stored in the browser.
(() => {
  const DATA_KEY='missionOSCloudData', META_KEY='missionOSCloudMeta', FOLDER='Mission Learning OS', FILE='mission-os-data.json', BACKUP_PREFIX='mission-os-backup-', SCOPES='https://www.googleapis.com/auth/drive.file';
  const TOKEN_KEY='missionOSDriveAccessToken', TOKEN_EXP_KEY='missionOSDriveAccessTokenExpiry';
  const get=(k,d)=>{try{return JSON.parse(localStorage.getItem(k)||'null')??d}catch{return d}};
  const put=(k,v)=>localStorage.setItem(k,JSON.stringify(v)); const now=()=>new Date().toISOString();
  const meta=()=>get(META_KEY,{revision:0,lastSync:null,lastBackup:null,cloudFileId:null,folderId:null,connected:false});
  const setMeta=p=>put(META_KEY,{...meta(),...p});
  const clientId=()=>window.MISSION_GOOGLE_CLIENT_ID||localStorage.getItem('missionOSGoogleClientId')||''; const configured=()=>!!clientId();
  function localSnapshot(){const data={};for(let i=0;i<localStorage.length;i++){const k=localStorage.key(i);if(!k||k===DATA_KEY||k===META_KEY||k===TOKEN_KEY||k===TOKEN_EXP_KEY)continue;if(k.startsWith('missionOS'))data[k]=get(k,null)}const m=meta();return{schemaVersion:1,revision:m.revision+1,updatedAt:now(),data}};
  function saveLocalRevision(){const s=localSnapshot();put(DATA_KEY,s);setMeta({revision:s.revision});return s}
  function applySnapshot(s){if(!s||typeof s!=='object'||!s.data)throw new Error('Invalid Mission OS backup.');Object.keys(s.data).forEach(k=>{if(k!==DATA_KEY&&k!==META_KEY&&k!==TOKEN_KEY&&k!==TOKEN_EXP_KEY)put(k,s.data[k])});put(DATA_KEY,s);setMeta({revision:Number(s.revision||0),lastSync:now()});return s}
  let tokenClient=null,accessToken=null,accessTokenExpiresAt=0;
  function loadStoredToken(){try{const t=localStorage.getItem(TOKEN_KEY)||'';const e=Number(localStorage.getItem(TOKEN_EXP_KEY)||0);if(t&&e>Date.now()+60000){accessToken=t;accessTokenExpiresAt=e;return true}localStorage.removeItem(TOKEN_KEY);localStorage.removeItem(TOKEN_EXP_KEY)}catch{}return false}
  function cacheToken(token,expiresIn=3600){accessToken=token;accessTokenExpiresAt=Date.now()+Math.max(60,Number(expiresIn||3600)-60)*1000;try{localStorage.setItem(TOKEN_KEY,accessToken);localStorage.setItem(TOKEN_EXP_KEY,String(accessTokenExpiresAt))}catch{}setMeta({connected:true})}
  function clearToken(){accessToken=null;accessTokenExpiresAt=0;try{localStorage.removeItem(TOKEN_KEY);localStorage.removeItem(TOKEN_EXP_KEY)}catch{}}
  function hasValidToken(){return !!accessToken&&accessTokenExpiresAt>Date.now()+30000}
  function loadGIS(){return new Promise((resolve,reject)=>{if(window.google?.accounts?.oauth2)return resolve();const s=document.createElement('script');s.src='https://accounts.google.com/gsi/client';s.onload=resolve;s.onerror=reject;document.head.appendChild(s)})}
  function requestToken(prompt=''){return new Promise((resolve,reject)=>{tokenClient=google.accounts.oauth2.initTokenClient({client_id:clientId(),scope:SCOPES,callback:r=>{if(r.error){clearToken();return reject(new Error(r.error_description||r.error||'Google authorization failed'))}cacheToken(r.access_token,r.expires_in);resolve({ok:true})}});tokenClient.requestAccessToken(prompt?{prompt}:{})})}
  async function connect(){if(!configured())throw new Error('Google OAuth Client ID is not configured.');if(hasValidToken()||loadStoredToken())return{ok:true,reused:true};await loadGIS();try{return await requestToken('')}catch(e){if(['interaction_required','login_required','consent_required'].includes(String(e.message).split(':')[0]))return requestToken('consent');throw e}}
  async function ensureToken(){if(hasValidToken()||loadStoredToken())return;if(!configured())throw new Error('Google OAuth Client ID is not configured.');await loadGIS();try{await requestToken('')}catch(e){throw new Error('Google Drive authorization has expired. Tap Connect Google once to re-authorize this browser.')}}
  function authHeaders(){if(!hasValidToken())throw new Error('Google Drive session is not active. Tap Connect Google once to authorize this device.');return{Authorization:`Bearer ${accessToken}`,'Content-Type':'application/json'}}
  async function api(url,opts={}){await ensureToken();let r=await fetch(url,{...opts,headers:{...authHeaders(),...(opts.headers||{})}});if(r.status===401){clearToken();await ensureToken();r=await fetch(url,{...opts,headers:{...authHeaders(),...(opts.headers||{})}})}if(!r.ok)throw new Error(`${r.status} ${await r.text()}`);return r.status===204?null:r.json()}
  async function findFolder(){const q=`name='${FOLDER.replace(/'/g,"\\'")}' and mimeType='application/vnd.google-apps.folder' and trashed=false`;const r=await api(`https://www.googleapis.com/drive/v3/files?q=${encodeURIComponent(q)}&spaces=drive&fields=files(id,name)`);return r.files?.[0]||null}
  async function ensureFolder(){let f=await findFolder();if(f)return f;return api('https://www.googleapis.com/drive/v3/files',{method:'POST',body:JSON.stringify({name:FOLDER,mimeType:'application/vnd.google-apps.folder'})})}
  async function findFile(folderId){const q=`name='${FILE}' and '${folderId}' in parents and trashed=false`;const r=await api(`https://www.googleapis.com/drive/v3/files?q=${encodeURIComponent(q)}&spaces=drive&fields=files(id,name,modifiedTime,version)`);return r.files?.[0]||null}
  async function uploadJson(fileId,folderId,snapshot){await ensureToken();if(fileId){const url=`https://www.googleapis.com/upload/drive/v3/files/${fileId}?uploadType=media&fields=id,name,modifiedTime,version`;const r=await fetch(url,{method:'PATCH',headers:{Authorization:`Bearer ${accessToken}`,'Content-Type':'application/json'},body:JSON.stringify(snapshot)});if(r.status===401){clearToken();await ensureToken();return uploadJson(fileId,folderId,snapshot)}if(!r.ok)throw new Error(`${r.status} ${await r.text()}`);return r.json()}const b='mission-os-boundary';const metadata={name:FILE,mimeType:'application/json',parents:[folderId]};const body=`--${b}\r\nContent-Type: application/json; charset=UTF-8\r\n\r\n${JSON.stringify(metadata)}\r\n--${b}\r\nContent-Type: application/json\r\n\r\n${JSON.stringify(snapshot)}\r\n--${b}--`;const r=await fetch('https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&fields=id,name,modifiedTime,version',{method:'POST',headers:{Authorization:`Bearer ${accessToken}`,'Content-Type':`multipart/related; boundary=${b}`},body});if(!r.ok)throw new Error(`${r.status} ${await r.text()}`);return r.json()}
  async function readRemote(id){return api(`https://www.googleapis.com/drive/v3/files/${id}?alt=media`)}
  async function backup(){const folder=await ensureFolder(),snapshot=saveLocalRevision(),name=`${BACKUP_PREFIX}${snapshot.updatedAt.replace(/[:.]/g,'-')}.json`,b='mission-os-backup-boundary';await ensureToken();const body=`--${b}\r\nContent-Type: application/json; charset=UTF-8\r\n\r\n${JSON.stringify({name,mimeType:'application/json',parents:[folder.id]})}\r\n--${b}\r\nContent-Type: application/json\r\n\r\n${JSON.stringify(snapshot)}\r\n--${b}--`;const r=await fetch('https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&fields=id,name,modifiedTime',{method:'POST',headers:{Authorization:`Bearer ${accessToken}`,'Content-Type':`multipart/related; boundary=${b}`},body});if(r.status===401){clearToken();await ensureToken();return backup()}if(!r.ok)throw new Error(`${r.status} ${await r.text()}`);const out=await r.json();setMeta({lastBackup:now(),folderId:folder.id});return out}
  async function listBackups(limit=12){const folder=await ensureFolder(),q=`'${folder.id}' in parents and name contains '${BACKUP_PREFIX}' and trashed=false`;const r=await api(`https://www.googleapis.com/drive/v3/files?q=${encodeURIComponent(q)}&spaces=drive&orderBy=createdTime desc&pageSize=${limit}&fields=files(id,name,createdTime,modifiedTime,size)`);return(r.files||[]).map(f=>({id:f.id,name:f.name,createdTime:f.createdTime,modifiedTime:f.modifiedTime,size:f.size||0}))}
  async function restoreBackup(id){const s=await readRemote(id);const a=applySnapshot(s);return{revision:a.revision,updatedAt:a.updatedAt}}
  async function restoreLatest(){const b=await listBackups(1);if(!b[0])throw new Error('No Mission OS backup found in Google Drive.');return restoreBackup(b[0].id)}
  async function sync(){const folder=await ensureFolder(),file=await findFile(folder.id),local=saveLocalRevision();if(!file){const c=await uploadJson(null,folder.id,local);setMeta({cloudFileId:c.id,folderId:folder.id,lastSync:now(),connected:true});return{direction:'upload',revision:local.revision,conflict:false}}const remote=await readRemote(file.id),rr=Number(remote?.revision||0),lr=Number(local.revision||0);if(rr>lr){applySnapshot(remote);setMeta({cloudFileId:file.id,folderId:folder.id,lastSync:now()});return{direction:'download',revision:rr,conflict:false}}if(rr===lr&&JSON.stringify(remote?.data||{})!==JSON.stringify(local?.data||{}))return{direction:'conflict',revision:lr,conflict:true};const u=await uploadJson(file.id,folder.id,local);setMeta({cloudFileId:u.id,folderId:folder.id,lastSync:now(),connected:true,revision:lr});return{direction:'upload',revision:lr,conflict:false}}
  function status(){const m=meta();const connected=hasValidToken()||loadStoredToken();return{configured:configured(),connected,authorized:!!m.connected,revision:m.revision,lastSync:m.lastSync,lastBackup:m.lastBackup,cloudFileId:m.cloudFileId}}
  loadStoredToken();
  window.missionOSGoogleDrive={version:'1.7',connect,sync,backup,status,saveLocalRevision,configured,listBackups,restoreBackup,restoreLatest};
})();
