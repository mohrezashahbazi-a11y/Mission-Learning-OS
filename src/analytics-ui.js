// Mission Learning OS — Analytics UI v1.1
(() => {
  const render=()=>{const a=window.missionOSAnalytics;if(!a)return;const review=document.getElementById('review');if(!review)return;let p=document.getElementById('analyticsPanel');if(!p){p=document.createElement('div');p.id='analyticsPanel';p.className='panel';p.style.marginTop='18px';const anchor=review.querySelector('.top');if(anchor)anchor.insertAdjacentElement('afterend',p);else review.prepend(p)}const s=a.summary(),days=a.last7Days(),subs=a.subjects(),m=a.mastery();const bars=Object.entries(days).map(([d,v])=>`<div style="display:flex;gap:10px;align-items:center;margin:6px 0"><span style="width:92px;font-size:12px">${d}</span><div style="height:10px;flex:1;background:var(--line,#ddd);border-radius:99px;overflow:hidden"><div style="height:100%;width:${Math.min(100,Math.round(v/120*100))}%;background:currentColor"></div></div><b>${Math.round(v)}m</b></div>`).join('');const subjectRows=subs.length?subs.map(x=>`<div class="stat"><div class="num">${x.minutes}m</div><div class="label">${x.id} · ${x.sessions} sessions</div></div>`).join(''):'<div class="empty">No completed Mission sessions yet.</div>';const masteryRows=Object.entries(m).length?Object.entries(m).map(([id,x])=>`<div class="stat"><div class="num">${Math.round(Number(x.score||0))}%</div><div class="label">${id} · ${x.level||'not_started'}</div></div>`).join(''):'<div class="empty">No evidence-based Mastery data yet.</div>';p.innerHTML=`<h2>Analytics</h2><div class="statgrid"><div class="stat"><div class="num">${s.actualMinutes}m</div><div class="label">Active study time</div></div><div class="stat"><div class="num">${s.sessions}</div><div class="label">Completed sessions</div></div><div class="stat"><div class="num">${s.completionRate}%</div><div class="label">Time vs plan</div></div></div><h3 style="margin-top:20px">Last 7 days</h3>${bars}<h3 style="margin-top:20px">By Mission</h3><div class="statgrid">${subjectRows}</div><h3 style="margin-top:20px">Mastery</h3><div class="statgrid">${masteryRows}</div>`};window.missionOSAnalyticsUI={version:'1.1',render};const boot=()=>{render();setInterval(render,10000)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();})();